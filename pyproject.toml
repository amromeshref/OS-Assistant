[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "os-assistant"
version = "0.1.0"
description = "AI OS Assistant"
authors = [{ name = "Amro Meshref" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]