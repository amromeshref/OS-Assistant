[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "os-hope"
version = "0.1.0"
description = "AI OS Assistant"
authors = [{ name = "" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
os-hope = "oshope.interfaces.cli:main"